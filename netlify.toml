[build]
  command = "npm ci && npx ng build"
  publish = "dist/bianca-waterpolo/browser"

[build.environment]
  NODE_VERSION = "20"

# Route all requests through the Angular SSR edge function
# Restrict to GET/HEAD so POSTs (e.g. Netlify Forms submissions) are handled by Netlify directly.
[[edge_functions]]
  path = "/*"
  function = "server"
  method = ["GET", "HEAD"]

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
